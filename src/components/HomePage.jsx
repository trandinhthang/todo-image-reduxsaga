import React from "react";

const HomePage = () => {
  const arrayID = [1, 2];
  const arrayProduct = [
    { id: 1, name: "Thang" },
    { id: 2, name: "Tran" },
    { id: 3, name: "Hoa" },
    { id: 4, name: "ABC" },
  ];

  // console.log(`arrayID`, arrayID);
  // console.log(`arrayProduct`, arrayProduct);

  // const res = arrayProduct.filter((item) => arrayID.includes(item.id));

  // console.log(`arrayProduct`, res);

  return <div>Trang chủ</div>;
};

export default HomePage;
